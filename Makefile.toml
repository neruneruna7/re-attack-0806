extend = [
    { path = "./Makefiles/train.toml"},
    { path = "./Makefiles/attack.toml"},
    { path = "./Makefiles/reattack.toml"},
    { path = "./Makefiles/prepross_reattack.toml" }
]

[config]
    skip_core_tasks=true


# 再攻撃の強度を変えると，どのような影響があるか
[tasks.prepross-cifar10-bim-gauss-fgsm-multi]
description = "CIFAR-10: BIM attack -> Gaussian Blur -> fgsm re-attack (複数の eps)"
script_runner = "@rust"
script = """
//! ```cargo
//! [dependencies]
//! ```

use std::fs::OpenOptions;
use std::io::Write;
use std::process::{Command, Stdio};

fn main() -> std::io::Result<()> {
    // 1. 設定: 変化させるパラメータと出力ファイル名
    let eps_values = vec!["2/255", "4/255", "8/255", "16/255"];
    let output_path = "experiment_log.txt";

    println!("実験を開始します。結果は {} に保存されます。", output_path);

    // 2. ログファイルの初期化（書き込みモードで開き、内容を空にする）
    // ループ内で追記するために、ファイルハンドルを保持します
    let mut file = OpenOptions::new()
        .create(true)
        .write(true)
        .truncate(true)
        .open(output_path)?;

    // 開始時刻などのヘッダーを書き込み
    writeln!(file, "=== Experiment Start ===")?;

    // 3. ループ処理
    for eps in eps_values {
        println!("実行中: eps = {} ...", eps);

        // ログファイルに見出し（区切り線）を書き込み
        writeln!(file, "\n------------------------------------------------------------")?;
        writeln!(file, "Parameter: reattack-eps = {}", eps)?;
        writeln!(file, "------------------------------------------------------------\n")?;

        // 4. コマンドの構築と実行
        // uv run ... の引数を設定
        let status = Command::new("uv")
            .args(&[
                "run", "src/general_preprocess_reattack.py",
                "--dataset", "cifar10",
                "--model", "morimoto-cifar10",
                "--attack-kind", "bim",
                "--attack-eps", "8/255",
                "--attack-alpha", "2/255",
                "--attack-n", "10",
                "--preprocess-kind", "gaussian_blur",
                "--preprocess-kernel-size", "3",
                "--preprocess-sigma", "0.5",
                "--reattack-kind", "fgsm",
                "--reattack-eps", eps,  // 変化させるパラメータ
                "--batch-size", "128",
                "--num-samples", "-1",
                "--no-save-images",
            ])
            // 標準出力とエラー出力を同じファイルハンドルに接続
            // try_clone()を使用して、各プロセスにファイルへの書き込み権限を複製して渡す
            .stdout(Stdio::from(file.try_clone()?))
            .stderr(Stdio::from(file.try_clone()?))
            .status()?;

        if !status.success() {
            eprintln!("警告: eps={} の実行中にエラーが発生しました", eps);
        }
    }

    writeln!(file, "\n=== Experiment Finished ===")?;
    println!("全ての実験が完了しました。");

    Ok(())
}
"""

# 再攻撃の強度を変えると，どのような影響があるか
[tasks.prepross-cifar10-bim-gauss-bim-multi]
description = "CIFAR-10: BIM attack -> Gaussian Blur -> BIM re-attack (複数の eps)"
command = "rust-script"
args = ["./Makefiles/scripts/prepross-cifar10-bim-gauss-bim-multi.rs"]

[tasks.prepross-cifar10-bim-lumine-bim-multi]
description = "CIFAR-10: BIM attack -> Lumine -> BIM re-attack (複数の eps)"
command = "rust-script"
args = ["./Makefiles/scripts/prepross-cifar10-bim-lumine-bim-multi.rs"]

# [tasks.prepross-cifar10-bim-my1-bim-multi]
# description = "CIFAR-10: BIM attack -> MyPreprocess1 -> BIM re-attack (複数の eps)"
# command = "rust-script"
# args = ["./Makefiles/scripts/prepross-cifar10-bim-my1-bim-multi.rs"]

[tasks.reattask-cifar10-bim-bim-multi]
description = "CIFAR-10: BIM attack -> BIM re-attack (複数の eps)"
command = "rust-script"
args = ["./Makefiles/scripts/reattask-cifar10-bim-bim-multi.rs"]
[tasks.reattask-cifar10-bim-fgsm-multi]
description = "CIFAR-10: BIM attack -> BIM re-attack (複数の eps)"
command = "rust-script"
args = ["./Makefiles/scripts/reattask-cifar10-bim-fgsm-multi.rs"]


[tasks.prepross-cifar10-bim-bim-gauss-multi]
description = "CIFAR-10: BIM attack -> Gaussian Blur -> BIM re-attack (複数の eps)"
command = "rust-script"
args = ["./Makefiles/scripts/prepross-cifar10-bim-bim-gauss-multi.rs"]

[tasks.prepross-cifar10-bim-bim-lumine-multi]
description = "CIFAR-10: BIM attack -> Lumine -> BIM re-attack (複数の eps)"
command = "rust-script"
args = ["./Makefiles/scripts/prepross-cifar10-bim-bim-lumine-multi.rs"]

[tasks.prepross-cifar10-bim-bim-my1-multi]
description = "CIFAR-10: BIM attack -> MyPreprocess1 -> BIM re-attack (複数の eps)"
command = "rust-script"
args = ["./Makefiles/scripts/prepross-cifar10-bim-bim-my1-multi.rs"]

[tasks.multi-multi]
run_task = [
    {name= "prepross-cifar10-bim-bim-gauss-multi" },
    {name= "prepross-cifar10-bim-bim-lumine-multi" },
    # {name= "prepross-cifar10-bim-bim-my1-multi" },
]

[tasks.metrics-pre-2-255]
description = "再攻撃前の各種指標を計算 (eps=2/255)"
command = "rust-script"
args = ["scripts/metrics_pre.rs", "preprocessed_reattacked_data/cifar10/resnet20/bim, epsilon: 0.03137254901960784, alpha: 0.00392156862745098, iters: 10/gaussian_blur, kernel_size: 3, sigma: 0.5/bim, epsilon: 0.00784313725490196, alpha: 0.00392156862745098, iters: 10/results.csv"]

[tasks.metrics-pre-4-255]
description = "再攻撃前の各種指標を計算 (eps=2/255)"
command = "rust-script"
args = ["scripts/metrics_pre.rs", "preprocessed_reattacked_data/cifar10/resnet20/bim, epsilon: 0.03137254901960784, alpha: 0.00392156862745098, iters: 10/gaussian_blur, kernel_size: 3, sigma: 0.5/bim, epsilon: 0.01568627450980392, alpha: 0.00392156862745098, iters: 10/results.csv"]

[tasks.metrics-pre-8-255]
description = "再攻撃前の各種指標を計算 (eps=2/255)"
command = "rust-script"
args = ["scripts/metrics_pre.rs", "preprocessed_reattacked_data/cifar10/resnet20/bim, epsilon: 0.03137254901960784, alpha: 0.00392156862745098, iters: 10/gaussian_blur, kernel_size: 3, sigma: 0.5/bim, epsilon: 0.03137254901960784, alpha: 0.00392156862745098, iters: 10/results.csv"]

[tasks.metrics-pre-16-255]
description = "再攻撃前の各種指標を計算 (eps=2/255)"
command = "rust-script"
args = ["scripts/metrics_pre.rs", "preprocessed_reattacked_data/cifar10/resnet20/bim, epsilon: 0.03137254901960784, alpha: 0.00392156862745098, iters: 10/gaussian_blur, kernel_size: 3, sigma: 0.5/bim, epsilon: 0.06274509803921569, alpha: 0.00392156862745098, iters: 10/results.csv"]

[tasks.ensenble]
command = "uv"
args = [
    "run",
    "src/ensemble_reattack.py",
    "--dataset",
    "cifar10",
    "--model",
    "resnet20",
    "--attack-eps",
    "4/255",
    "--attack-alpha",
    "1/255",
    "--attack-n",
    "10",
    "--reattack-eps",
    "4/255",
    "--reattack-alpha",
    "1/255",
    "--reattack-n",
    "10",
    "--batch-size",
    "128",
]